# 🎯 دليل التشغيل السريع - بوت رفع جودة الصور 4K

## ⚡️ التشغيل في 3 خطوات

### الخطوة 1️⃣: تحميل المشروع

**أنت الآن تملك الملف المضغوط الكامل!**

قم بفك الضغط في أي مكان:
```bash
unzip telegram_upscaler_bot.zip
cd telegram_upscaler_bot
```

### الخطوة 2️⃣: الإعداد

#### أ) الحصول على Bot Token:

1. افتح تيليجرام
2. ابحث عن `@BotFather`
3. أرسل `/newbot`
4. اختر اسماً للبوت (مثال: My Upscaler Bot)
5. اختر username للبوت (مثال: my_upscaler_bot)
6. **احفظ التوكن** الذي سيرسله لك

#### ب) الحصول على API Key (مجاني):

**الخيار 1: ClipDrop (موصى به) - 100 صورة/شهر مجاناً**

1. اذهب إلى: https://clipdrop.co/apis
2. اضغط "Sign Up" وسجل حساباً
3. اذهب إلى "API Keys"
4. انسخ مفتاح API

**الخيار 2: DeepAI - 500 طلب/شهر مجاناً**

1. اذهب إلى: https://deepai.org/
2. سجل حساباً مجانياً
3. اذهب إلى Profile → API Keys
4. انسخ المفتاح

**الخيار 3: Replicate (متقدم)**

1. اذهب إلى: https://replicate.com/
2. سجل بحساب GitHub
3. احصل على API Token من الإعدادات

#### ج) تكوين البوت:

أنشئ ملف `.env` (انسخ من `.env.example`):

```bash
cp .env.example .env
```

افتح ملف `.env` بأي محرر نصوص وضع بياناتك:

```env
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz-1234567
API_KEY=your_clipdrop_or_deepai_api_key_here
UPSCALE_API_ENDPOINT=https://clipdrop-api.co/image-upscaling/v1/upscale
```

### الخطوة 3️⃣: التشغيل

#### على Linux/Mac:
```bash
chmod +x start.sh
./start.sh
```

#### على Windows:
```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python main.py
```

#### باستخدام Docker (اختياري):
```bash
docker build -t telegram-upscaler .
docker run -d --env-file .env telegram-upscaler
```

---

## 🎉 البوت يعمل الآن!

عند رؤية هذه الرسالة:
```
🚀 جاري تشغيل البوت...
🔑 API Key configured: ✅
INFO:aiogram.dispatcher:Start polling
```

**تهانينا! البوت جاهز للاستخدام! 🎊**

---

## 📱 كيفية الاستخدام

### 1. افتح البوت في تيليجرام

ابحث عن username البوت الذي أنشأته (مثال: @my_upscaler_bot)

### 2. ابدأ المحادثة

أرسل: `/start`

سترى رسالة ترحيب مع التعليمات

### 3. أرسل صورة

**طريقتان:**

**أ) صورة عادية:**
- اضغط على أيقونة المرفقات
- اختر "Photo"
- اختر الصورة

**ب) كملف (جودة أفضل):**
- اضغط على أيقونة المرفقات
- اختر "File"
- اختر الصورة
- ✅ **هذه الطريقة أفضل!**

### 4. انتظر المعالجة

سترى رسالة: "⏳ جاري المعالجة..."

وقت المعالجة: 30-90 ثانية حسب حجم الصورة

### 5. استلم النتيجة!

ستستلم الصورة المحسنة بجودة 4K كملف 📄

---

## 🔧 استكشاف الأخطاء

### ❌ خطأ: "BOT_TOKEN غير موجود"

**الحل:**
- تأكد من إنشاء ملف `.env`
- تأكد من كتابة `BOT_TOKEN=` بشكل صحيح
- لا تضع مسافات حول `=`

### ❌ خطأ: "API Error 401"

**الحل:**
- تحقق من صحة `API_KEY`
- جرب إنشاء مفتاح جديد
- تأكد من نسخ المفتاح كاملاً

### ❌ خطأ: "الصورة كبيرة جداً"

**الحل:**
- ClipDrop يدعم حتى 10MB
- قلّص حجم الصورة قبل الإرسال
- جرب خدمة API أخرى

### ❌ البوت لا يستجيب

**الحل:**
```bash
# تحقق من السجلات
# ابحث عن أي رسائل خطأ باللون الأحمر

# أعد تشغيل البوت
# اضغط Ctrl+C لإيقافه
# ثم شغّله مرة أخرى
python main.py
```

### ❌ خطأ: "ModuleNotFoundError"

**الحل:**
```bash
# تأكد من تثبيت جميع المكتبات
pip install -r requirements.txt

# أو ثبت المكتبة المفقودة مباشرة
pip install aiogram aiohttp python-dotenv
```

---

## 💡 نصائح للاستخدام الأمثل

### ✅ للحصول على أفضل جودة:
1. أرسل الصورة **كملف** وليس كصورة عادية
2. استخدم صور بجودة جيدة أصلاً (كلما كانت الصورة الأصلية أفضل، كانت النتيجة أفضل)
3. الصور الصغيرة تُعالج أسرع

### ✅ تحسين الأداء:
- شغّل البوت على سيرفر ثابت (انظر HOSTING_GUIDE.md)
- استخدم API سريع (ClipDrop هو الأسرع)
- تجنب معالجة صور كبيرة جداً (فوق 10MB)

### ✅ الحفاظ على الحد المجاني:
- ClipDrop: 100 صورة/شهر
- DeepAI: 500 طلب/شهر
- راقب استهلاكك من لوحة تحكم API

---

## 🚀 خطوات متقدمة (اختيارية)

### تغيير واجهة البوت:

عدّل ملف `main.py` في دالة `cmd_start()`:

```python
@dp.message(Command("start"))
async def cmd_start(message: types.Message):
    welcome_text = """
🎨 <b>ضع رسالتك الترحيبية هنا!</b>
    """
    await message.answer(welcome_text)
```

### إضافة لغات متعددة:

```python
# أضف في بداية الملف
LANGUAGES = {
    'ar': {'welcome': 'مرحباً!'},
    'en': {'welcome': 'Welcome!'}
}
```

### إضافة إحصائيات:

```python
# أضف متغير عام
processed_images = 0

# في دالة handle_photo
processed_images += 1
logger.info(f"Total processed: {processed_images}")
```

---

## 📊 مراقبة الأداء

### عرض السجلات:

```bash
# أثناء التشغيل، ستظهر السجلات مباشرة
# للحفظ في ملف:
python main.py > bot.log 2>&1

# لعرض السجلات الحية:
tail -f bot.log
```

### مؤشرات الأداء:

```
✅ علامات النجاح:
- "Start polling" - البوت يعمل
- "Processing photo" - استلام صورة
- "Upscale successful" - معالجة ناجحة

❌ علامات المشاكل:
- "API Error" - مشكلة في API
- "Timeout" - الصورة كبيرة/الإنترنت بطيء
- "401/403" - مشكلة في API Key
```

---

## 🎓 موارد إضافية

### الوثائق:
- [aiogram Documentation](https://docs.aiogram.dev/)
- [ClipDrop API Docs](https://clipdrop.co/apis/docs)
- [Telegram Bot API](https://core.telegram.org/bots/api)

### دعم فني:
- **مشاكل في الكود:** راجع README.md
- **مشاكل في الاستضافة:** راجع HOSTING_GUIDE.md
- **مشاكل في API:** تواصل مع دعم الخدمة

### تحديثات:
راقب المشروع على GitHub للحصول على آخر التحديثات

---

## 🎯 الخطوات التالية

بعد تشغيل البوت بنجاح:

1. ✅ **جرّب البوت** بإرسال صور مختلفة
2. ✅ **راجع دليل الاستضافة** لنشر البوت على الإنترنت
3. ✅ **خصص البوت** حسب احتياجاتك
4. ✅ **شارك البوت** مع الأصدقاء!

---

<div align="center">

## 🎊 استمتع باستخدام البوت!

**إذا واجهت أي مشكلة، راجع قسم استكشاف الأخطاء أعلاه**

صُنع بـ ❤️ باستخدام Python و aiogram

---

**📚 وثائق المشروع:**
- `README.md` - دليل شامل
- `HOSTING_GUIDE.md` - دليل الاستضافة المجانية
- `QUICK_START.md` - هذا الملف

</div>
