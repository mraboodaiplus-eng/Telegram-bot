[package]
name = "omega_royal"
version = "2.1.0"
edition = "2021"

# التوجيه الصارم لملف الكود
[[bin]]
name = "omega_royal"
path = "main.rs"

[dependencies]
tokio = { version = "1", features = ["full"] }
reqwest = { version = "0.11", features = ["json", "rustls-tls"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio-tungstenite = { version = "0.20", features = ["rustls-tls-native-roots"] }
futures-util = "0.3"
dotenv = "0.15"
hmac = "0.12"
sha2 = "0.10"
hex = "0.4"
url = "2.4"
chrono = "0.4"
warp = "0.3"           # للخادم الوهمي (Render Port)
jemallocator = "0.5"   # 🚀 مسرع الذاكرة الخارق

# ⚡ إعدادات الأداء القصوى (Rust Compiler Optimizations) ⚡
[profile.release]
opt-level = 3       # أقصى درجات التحسين
lto = true          # Link Time Optimization (دمج الكود لتقليل القفزات)
codegen-units = 1   # تقليل الوحدات لزيادة سرعة الكود الناتج (على حساب وقت البناء)
panic = "abort"     # إيقاف فوري عند الخطأ لتقليل حجم الكود
strip = true        # إزالة الرموز الزائدة