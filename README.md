# 🎯 Omega Predator Trading Bot

بوت تداول عالي السرعة على منصة MEXC مع استراتيجية "صيد الحيتان" المتقدمة.

## 📋 المميزات

- **سرعة فائقة**: استخدام WebSocket للمراقبة الفورية
- **استراتيجية ذكية**: شراء عند ارتفاع 5% خلال 20 ثانية
- **حماية رأس المال**: بيع تلقائي عند تراجع 3% من الذروة
- **برمجة غير متزامنة**: استخدام asyncio لأقصى كفاءة
- **تحكم عبر Telegram**: إدارة سهلة وإشعارات فورية

## 🚀 التثبيت

### المتطلبات

- Python 3.11 أو أحدث
- حساب على منصة MEXC مع API Keys
- Telegram Bot Token

### خطوات التثبيت

1. **استنساخ المشروع**
```bash
git clone <repository-url>
cd omega-predator
```

2. **تثبيت المكتبات**
```bash
pip install -r requirements.txt
```

3. **إعداد المتغيرات البيئية**
```bash
cp .env.example .env
```

ثم قم بتعديل ملف `.env` وأضف:
- `MEXC_API_KEY`: مفتاح API من MEXC
- `MEXC_SECRET_KEY`: المفتاح السري من MEXC
- `TELEGRAM_BOT_TOKEN`: توكن البوت من BotFather
- `TELEGRAM_CHAT_ID`: معرف المحادثة الخاص بك

## 📖 الاستخدام

### تشغيل البوت

```bash
python main.py
```

### التحكم عبر Telegram

1. أرسل `/start` للبوت عبر Telegram
2. سيطلب منك البوت تحديد مبلغ الصفقة بالدولار
3. أدخل المبلغ (مثال: `100`)
4. سيبدأ البوت في المراقبة والتداول تلقائيًا

## ⚙️ الإعدادات

يمكنك تعديل الإعدادات في ملف `config.py`:

- `BUY_THRESHOLD`: عتبة الشراء (افتراضي: 0.05 = 5%)
- `SELL_THRESHOLD`: عتبة البيع (افتراضي: 0.03 = 3%)
- `TIME_WINDOW`: النافذة الزمنية (افتراضي: 20 ثانية)
- `WHITELIST`: قائمة العملات المراقبة

## 🏗️ البنية المعمارية

```
omega-predator/
├── main.py                 # نقطة الدخول الرئيسية
├── config.py              # الإعدادات والمتغيرات
├── trading_logic.py       # منطق التداول
├── mexc_handler.py        # معالج MEXC API
├── websocket_handler.py   # معالج WebSocket
├── telegram_handler.py    # معالج Telegram
├── requirements.txt       # المكتبات المطلوبة
├── .env.example          # مثال للمتغيرات البيئية
└── README.md             # هذا الملف
```

## 🔒 الأمان

- **لا تشارك ملف `.env` أبدًا**
- احفظ مفاتيح API في مكان آمن
- استخدم API Keys بصلاحيات محدودة (التداول فقط)
- راقب نشاط الحساب بانتظام

## 📊 استراتيجية التداول

### شرط الشراء
- مراقبة الأسعار خلال نافذة زمنية 20 ثانية
- عند ارتفاع السعر بنسبة 5% أو أكثر: شراء فوري

### شرط البيع
- تتبع أعلى سعر (الذروة) بعد الشراء
- عند تراجع السعر 3% من الذروة: بيع فوري
- يطبق حتى لو كانت الصفقة خاسرة (حماية رأس المال)

## ⚡ تحسينات الأداء

- استخدام `asyncio` لجميع العمليات
- `collections.deque` للتخزين السريع
- عدم وجود عمليات blocking في الحلقة الرئيسية
- التسجيل بعد التنفيذ وليس قبله

## 🚨 تحذيرات

- **التداول يحمل مخاطر**: قد تخسر رأس المال
- **اختبر أولاً**: استخدم حساب تجريبي قبل التداول الحقيقي
- **راقب باستمرار**: لا تترك البوت دون مراقبة
- **ابدأ بمبالغ صغيرة**: حتى تتأكد من عمل البوت بشكل صحيح

## 📝 الترخيص

هذا المشروع للاستخدام الشخصي. استخدمه على مسؤوليتك الخاصة.

## 👨‍💻 المطور

تم تطويره بواسطة **CodeMaestro** بأمر من السيد مارك.

---

**ملاحظة**: هذا البوت أداة تداول آلية. النتائج السابقة لا تضمن النتائج المستقبلية. استخدمه بحذر ومسؤولية.
