# 🎨 بوت تيليجرام لرفع جودة الصور إلى 4K
## Telegram 4K Image Upscaler Bot

<div dir="rtl">

بوت تيليجرام احترافي مجاني لرفع جودة الصور إلى دقة 4K باستخدام تقنيات الذكاء الاصطناعي.

## ✨ المميزات

- 🚀 رفع جودة الصور إلى 4K بشكل احترافي
- 🎯 الحفاظ على الملامح والتفاصيل الأصلية
- ⚡️ معالجة سريعة وغير متزامنة
- 📤 إرسال النتائج كملفات عالية الجودة
- 💰 **مجاني بالكامل** (100 صورة/شهر مع ClipDrop)
- 🔒 آمن ولا يحفظ الصور

## 🛠 التقنيات المستخدمة

- **Python 3.9+**
- **aiogram 3.x** - مكتبة حديثة للتعامل مع Telegram Bot API
- **aiohttp** - طلبات HTTP غير متزامنة
- **ClipDrop API** - API مجاني لرفع جودة الصور

## 📦 التثبيت والإعداد

### 1. متطلبات النظام

```bash
# تأكد من تثبيت Python 3.9 أو أحدث
python --version
```

### 2. استنساخ المشروع

```bash
# إنشاء مجلد المشروع
mkdir telegram_upscaler_bot
cd telegram_upscaler_bot
```

### 3. تثبيت المكتبات

```bash
# إنشاء بيئة افتراضية (موصى به)
python -m venv venv

# تفعيل البيئة الافتراضية
# في Windows:
venv\Scripts\activate
# في Linux/Mac:
source venv/bin/activate

# تثبيت المتطلبات
pip install -r requirements.txt
```

### 4. إعداد ملف البيئة

```bash
# نسخ ملف المثال
cp .env.example .env

# تحرير الملف وإضافة بياناتك
nano .env  # أو أي محرر نصوص
```

**محتوى ملف `.env`:**

```env
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
API_KEY=your_clipdrop_api_key_here
```

## 🔑 الحصول على البيانات المطلوبة

### الحصول على Bot Token:

1. افتح تيليجرام وابحث عن [@BotFather](https://t.me/BotFather)
2. أرسل الأمر `/newbot`
3. اتبع التعليمات واختر اسماً للبوت
4. احفظ التوكن الذي سيرسله لك

### الحصول على ClipDrop API Key (مجاني):

1. اذهب إلى [ClipDrop APIs](https://clipdrop.co/apis)
2. سجل حساباً جديداً (مجاني)
3. اذهب إلى صفحة API Keys
4. انسخ مفتاح API الخاص بك
5. **الخطة المجانية:** 100 صورة شهرياً

### بدائل مجانية أخرى:

#### 1. Replicate API (مجاني للاستخدام المحدود):
```env
API_KEY=r8_your_replicate_token
UPSCALE_API_ENDPOINT=https://api.replicate.com/v1/predictions
```

#### 2. DeepAI (مجاني - 500 طلب/شهر):
```env
API_KEY=your_deepai_key
UPSCALE_API_ENDPOINT=https://api.deepai.org/api/waifu2x
```

## 🚀 تشغيل البوت

```bash
# تأكد من تفعيل البيئة الافتراضية أولاً
python main.py
```

عند التشغيل بنجاح، ستظهر رسالة:
```
🚀 جاري تشغيل البوت...
🔑 API Key configured: ✅
```

## 📱 استخدام البوت

1. **افتح البوت في تيليجرام**
2. **أرسل `/start` للبدء**
3. **أرسل أي صورة** (كصورة عادية أو ملف)
4. **انتظر المعالجة** (30-60 ثانية)
5. **استلم الصورة بجودة 4K!**

### الأوامر المتاحة:

- `/start` - بدء استخدام البوت
- `/help` - عرض دليل الاستخدام

## 🖥 الاستضافة المجانية

### 1. Render.com (موصى به):

```yaml
# إنشاء ملف render.yaml
services:
  - type: web
    name: telegram-upscaler-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: API_KEY
        sync: false
```

**الخطوات:**
1. سجل في [Render.com](https://render.com)
2. اربط حساب GitHub
3. أنشئ Web Service جديد
4. اختر المشروع وأضف المتغيرات البيئية
5. انشر المشروع (مجاني!)

### 2. Railway.app:

```bash
# تثبيت Railway CLI
npm i -g @railway/cli

# تسجيل الدخول
railway login

# نشر المشروع
railway init
railway up
```

### 3. PythonAnywhere (مجاني مع قيود):

1. سجل في [PythonAnywhere](https://www.pythonanywhere.com)
2. ارفع الملفات عبر Files
3. افتح Bash Console وثبت المكتبات
4. شغل البوت من Dashboard

### 4. Heroku (خطة مجانية محدودة):

```bash
# تثبيت Heroku CLI ثم:
heroku login
heroku create telegram-upscaler-bot
git push heroku main
heroku ps:scale worker=1
```

## 📂 هيكل المشروع

```
telegram_upscaler_bot/
│
├── main.py                 # الكود الرئيسي للبوت
├── requirements.txt        # المكتبات المطلوبة
├── .env                    # المتغيرات البيئية (لا تشاركه!)
├── .env.example           # مثال لملف البيئة
├── README.md              # هذا الملف
└── .gitignore             # ملفات مستثناة من Git
```

## 🔧 تخصيص البوت

### تغيير API الرفع:

في ملف `main.py`، عدّل دالة `upscale_image()`:

```python
async def upscale_image(image_data: bytes, api_key: str) -> bytes:
    # ضع كود API الجديد هنا
    headers = {'Authorization': f'Bearer {api_key}'}
    # ... باقي الكود
```

### إضافة ميزات جديدة:

```python
@dp.message(Command("settings"))
async def cmd_settings(message: types.Message):
    """إعدادات البوت"""
    # أضف إعدادات مخصصة هنا
    pass
```

## 🐛 حل المشاكل

### المشكلة: "BOT_TOKEN غير موجود"
**الحل:** تأكد من إنشاء ملف `.env` وإضافة التوكن

### المشكلة: "API Error 401"
**الحل:** تحقق من صحة `API_KEY` في ملف `.env`

### المشكلة: "الصورة كبيرة جداً"
**الحل:** 
- ClipDrop يدعم حتى 10MB
- قلل حجم الصورة قبل الإرسال
- استخدم خدمة بديلة

### المشكلة: "البوت لا يستجيب"
**الحل:**
```bash
# تحقق من السجلات
tail -f bot.log

# أعد تشغيل البوت
python main.py
```

## 📊 الحدود المجانية

| الخدمة | الحد المجاني | السعر بعد الحد |
|--------|--------------|----------------|
| ClipDrop | 100 صورة/شهر | 0.05$/صورة |
| DeepAI | 500 طلب/شهر | 5$/5000 طلب |
| Replicate | 100 دقيقة | حسب الاستخدام |

## 🔐 الأمان

- ❌ لا تشارك ملف `.env` أبداً
- ✅ استخدم `.gitignore` لمنع رفع البيانات الحساسة
- ✅ غيّر المفاتيح إذا تعرضت للتسريب
- ✅ استخدم HTTPS فقط للـ webhooks

## 📝 الترخيص

هذا المشروع مفتوح المصدر ومجاني للاستخدام الشخصي والتجاري.

## 🤝 المساهمة

نرحب بالمساهمات! لتحسين المشروع:

1. Fork المشروع
2. أنشئ فرعاً جديداً (`git checkout -b feature/AmazingFeature`)
3. Commit التغييرات (`git commit -m 'Add some AmazingFeature'`)
4. Push للفرع (`git push origin feature/AmazingFeature`)
5. افتح Pull Request

## 📞 الدعم

- **الأسئلة؟** افتح Issue على GitHub
- **اقتراحات؟** أرسل Pull Request
- **أخطاء؟** أبلغ عنها في Issues

## 🌟 المطور

تم التطوير بواسطة مساعد الذكاء الاصطناعي من Genspark

---

<div align="center">

**⭐️ إذا أعجبك المشروع، لا تنسَ تقييمه بنجمة!**

صُنع بـ ❤️ باستخدام Python و aiogram

</div>

</div>
